<%= form_with(model: water, local: true) do |form| %>
  <% if water.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(water.errors.count, "error") %> prohibited this water from being saved:</h2>

      <ul>
        <% water.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :feeling %>
    <%= form.text_field :feeling %>
  </div>

  <div class="field">
    <%= form.label :intensity %>
    <%= form.number_field :intensity %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
